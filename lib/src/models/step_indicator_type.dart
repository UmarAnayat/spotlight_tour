/// Style of the built-in step progress indicator.
enum StepIndicatorType {
  /// Text counter, e.g. "Step 2 of 5".
  text,

  /// Animated dot row.
  dots,

  /// Linear progress bar with percentage.
  linear,
}
