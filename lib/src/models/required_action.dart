/// The gesture a user must perform on the target widget to advance.
enum RequiredAction {
  /// User must tap the target once.
  tap,

  /// User must double-tap the target.
  doubleTap,

  /// User must long-press the target.
  longPress,
}
